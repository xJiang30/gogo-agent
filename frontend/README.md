# Gogo Agent Frontend

This is the clean product frontend workspace. The static prototype remains in
`../demo` and should be treated as a UX reference, not as production source.

## Commands

```bash
npm install
npm run dev
npm run build
```

Set `VITE_API_BASE_URL` when the backend is not running on
`http://127.0.0.1:8000`.
