# Gogo Agent Demo

This folder contains the static UX prototype. It is the reference for product
flow and interaction details, not the production frontend source.

## Commands

```bash
python3 -m http.server 8765 --directory demo
node demo/tests/ux-regression.test.mjs
```
